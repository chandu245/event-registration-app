# Copy this file to .env and set real values — .env is gitignored and never committed.
# Used only for local `docker compose up` testing (Stage 1). Production credentials
# come from Jenkins credentials, never from a file in this repo.
MYSQL_ROOT_PASSWORD=change-this-root-password
MYSQL_PASSWORD=change-this-app-password
